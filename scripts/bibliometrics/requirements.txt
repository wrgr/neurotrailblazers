# Bibliometric pipeline dependencies
# Install with: pip install -r requirements.txt
requests>=2.28
networkx>=3.0
numpy>=1.24
scipy>=1.10
anthropic>=0.40     # step 8 (OCAR generation) only — requires ANTHROPIC_API_KEY
