---
layout: null
permalink: /technical-training/journal-club/graph-data.json
sitemap: false
---
[
{%- for p in site.data.journal_papers.papers -%}
{"id":{{ p.id | jsonify }},"title":{{ p.title | jsonify }},"authors":{{ p.authors | jsonify }},"year":{{ p.year | jsonify }},"journal":{{ p.journal | jsonify }},"doi":{{ p.doi | jsonify }},"dimension":{{ p.dimension | jsonify }},"era":{{ p.inclusion_role | jsonify }},"tier":{{ p.tier | default: 2000 | jsonify }},"in_degree":{{ p.in_degree | default: 0 | jsonify }},"out_degree":{{ p.out_degree | default: 0 | jsonify }},"kcore":{{ p.k_core | default: 5 | jsonify }},"organism":{{ p.tags | jsonify }},"citation_role":{{ p.citation_role | jsonify }},"scope_role":{{ p.scope_role | jsonify }},"summary":{{ p.plain_language_summary | jsonify }},"citation":{{ p.citation | jsonify }},"cites":{{ p.cites | jsonify }}}{% unless forloop.last %},{% endunless %}
{%- endfor -%}
]
